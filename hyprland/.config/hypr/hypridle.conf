listener {
    timeout = 300
    on-timeout = hyprctl dispatch dpms off && ~/.local/bin/lock # command to run when timeout has passed
    on-resume = hyprctl dispatch dpms on  # command to run when activity is detected after timeout has fired.
}
